import { TsRestErrorInterceptorInterceptor } from './ts-rest-error-interceptor.interceptor';

describe('TsRestErrorInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TsRestErrorInterceptorInterceptor()).toBeDefined();
  });
});
